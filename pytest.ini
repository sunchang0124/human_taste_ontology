[pytest]
testpaths = tests
# `network` marks a test that reaches a live service (the OLS4 API). It is
# deselected by default so the suite runs offline and deterministically; run
# it explicitly with `python3 -m pytest -m network`.
markers =
    network: requires live network access (OLS4)
addopts = -m "not network"
